{% extends 'JSAdminBundle:Default:index.html.twig' %}
{% block header %}
    <h1>Список категорий</h1>
{% endblock %}
{% block list %}
    <table class="table table-bordered table-striped">
        <tr>
            <th>Имя</th>
            <th></th>
        </tr>

        {% for item in all %}
            <tr>
                <td>{{ item.name }}</td>
                <td>
                    <a onclick="return confirm('Вы действительно хотите удалить запись?')" class="btn btn-mini" href="{{ url('admin_category_delete', {'pk' : item.id}) }}"><i class="icon-remove"></i> Удалить</a>
                    <a class="btn btn-mini" href="{{ url('admin_category_edit', {'pk' : item.id}) }}"><i class="icon-pencil"></i> Редактировать</a>
                </td>

            </tr>
        {% endfor %}

    </table>
{% endblock %}
